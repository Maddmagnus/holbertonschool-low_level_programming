#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints number of coins
 * @argc: number of args
 * @argv: the array
 * Return: number of coins needed
 */

int main(int argc, char *argv[])
{
	int coins = 0;

	if (argc != 2)
	{
		printf("Error\n");
		return (1);
	}
	if (sum < 0)
	{
		printf("0\n")
			}
	printf("%d\n", coins);
	return (0);
}
